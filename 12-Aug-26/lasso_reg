from sklearn.linear_model import Lasso

X = [[1], [2], [3], [4], [5], [6], [7], [8]]
y = [2, 4, 5, 8, 10, 12, 14, 16]

model = Lasso(alpha=0.1)

model.fit(X, y)

print("Coefficient:", model.coef_[0])
print("Intercept:", model.intercept_)

print("Prediction for 5:", model.predict([[10]])[0])
